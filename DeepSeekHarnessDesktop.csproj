<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows10.0.19041.0</TargetFramework>
    <RootNamespace>DeepSeekHarnessDesktop</RootNamespace>
    <AssemblyName>DeepSeekHarnessDesktop</AssemblyName>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <UseWPF>true</UseWPF>
    <UseWindowsForms>true</UseWindowsForms>
    <EnableWindowsTargeting>true</EnableWindowsTargeting>
    <Platforms>x64</Platforms>
    <PlatformTarget>x64</PlatformTarget>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <ApplicationManifest>app.manifest</ApplicationManifest>
    <Version>1.0.0</Version>
    <FileVersion>1.0.0.0</FileVersion>
    <AssemblyVersion>1.0.0.0</AssemblyVersion>
    <AssemblyTitle>DeepSeek Harness Desktop</AssemblyTitle>
    <Product>DeepSeek Harness Desktop</Product>
    <Description>DeepSeek Harness Windows desktop client</Description>
    <ApplicationIcon Condition="Exists('app.ico')">app.ico</ApplicationIcon>
    <!-- Installer staging can contain third-party source files. It is never
         application source and must not be picked up by SDK default globs. -->
    <DefaultItemExcludes>$(DefaultItemExcludes);installer-staging/**;artifacts/**;installer/**/bin/**;installer/**/obj/**</DefaultItemExcludes>
    <!-- XML reference documentation is not a runtime asset. Excluding it also
         keeps single-file publish from mapping several WebView2 docs to one
         bundle destination on the first publish into a clean directory. -->
    <AllowedReferenceRelatedFileExtensions>.pdb;.pri;.dll.config;.exe.config</AllowedReferenceRelatedFileExtensions>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Web.WebView2" Version="1.0.3537.50" />
  </ItemGroup>

  <ItemGroup>
    <!-- The bootstrapper is its own SDK project. Its generated C# files must
         never become part of the WPF desktop application's default glob. -->
    <Compile Remove="installer\**\*.cs" />
  </ItemGroup>
</Project>
